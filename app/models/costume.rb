# Create your Costume class here
# It should inherit from ActiveRecord::Base

# ~~~~~~~~~~~~~~~ use t.timestamps at the end so it automatically makes created_at and updated_at columns 

class Costume < ActiveRecord::Base 
    
end 